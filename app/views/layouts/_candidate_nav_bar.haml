%header.tr-header
  %nav.navbar.navbar-default
    .container
      .navbar-header
        %a.navbar-brand{:href => "/"}
          %img.img-responsive= image_tag("woods_logo.jpg")
      / /.navbar-header
      .navbar-right
        %ul.sign-in.tr-list
          %li= link_to "About Us |", about_us_path
          %li= link_to "Contact Us |", contact_us_path
          %li= link_to "Job Vacancies |", job_vacancies_path
          %li= link_to "Profile |", candidate_path(id: current_candidate.id)

          %li= link_to "Edit Profile |", edit_candidate_registration_path(id: current_candidate.id)

          %li
            -if current_candidate.present?
              = link_to "Sign Out", destroy_candidate_session_path,:method => :delete
            -else
              = link_to "Sign In", new_candidate_session_path


        / /.sign-in
        -# %a.btn.btn-primary{:href => "job-post.html"} Post Job
      / /.nav-right
    / /.container
  / /.navbar
/ /.tr-header
